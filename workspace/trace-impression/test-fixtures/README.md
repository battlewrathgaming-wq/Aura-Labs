# Trace Impression Test Fixtures

Status: local test fixtures

This folder holds alternate Trace Impression context inputs for controlled behavior checks.

Fixtures must not replace `workspace/trace-impression/context.md`.

Do not treat fixture output as accepted project state unless a separate Human / Overseer review explicitly accepts it.
